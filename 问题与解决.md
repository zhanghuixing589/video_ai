# 问题与解决

本文档用于持续记录项目开发中遇到的问题、原因、解决方案、验证方式和后续改进。后续发现新问题时，继续按相同格式追加。

## 记录模板

### 问题标题

- **日期**：YYYY-MM-DD
- **现象**：
- **影响**：
- **根因**：
- **解决方案**：
- **验证方式**：
- **后续改进**：

---

## 1. 前端请求地址与后端接口不匹配

- **日期**：2026-06-12
- **现象**：前端请求 `/api/auth/login` 时无法正确访问后端登录接口。
- **影响**：登录、注册以及其他 API 请求失败。
- **根因**：后端配置了 `/api` context path，但 Vite 代理又将 `/api` 前缀删除，最终请求路径错误。
- **解决方案**：保留 Vite 代理中的 `/api` 前缀，只将请求转发到 `http://localhost:8080`。
- **验证方式**：浏览器访问登录页并调用登录接口；公开内容接口返回 HTTP 200。
- **后续改进**：使用环境变量统一管理不同环境的 API 地址。

## 2. 前端错误解析统一响应结构

- **日期**：2026-06-12
- **现象**：登录成功后读取不到 `token` 和用户信息。
- **影响**：无法保存登录状态，也无法按照用户角色跳转。
- **根因**：后端响应结构是 `{ success, message, data }`，前端却直接把外层响应当作业务数据。
- **解决方案**：在 Axios API 层统一解包 `response.data.data`，页面只接收业务对象。
- **验证方式**：管理员登录后可获得 JWT，并自动进入 `/admin`。
- **后续改进**：根据 OpenAPI 自动生成前端接口类型，减少前后端契约不一致。

## 3. TypeScript 配置与编译器版本不兼容

- **日期**：2026-06-12
- **现象**：执行 `npm run build` 时出现 `erasableSyntaxOnly` 未知配置，以及 `tsBuildInfoFile` 缺少增量编译配置。
- **影响**：前端无法构建。
- **根因**：脚手架生成的配置项高于项目锁定的 TypeScript 5.5 能力。
- **解决方案**：删除不支持的 `erasableSyntaxOnly` 和不必要的 `tsBuildInfoFile` 配置。
- **验证方式**：`npm run build` 成功完成。
- **后续改进**：升级依赖时同时检查 TypeScript、Vite 和脚手架配置版本。

## 4. 登录页面角色选择器无实际作用

- **日期**：2026-06-12
- **现象**：登录页面允许选择普通用户、制片厂、审核员和管理员，但该选择没有发送给后端。
- **影响**：容易让用户误以为可以自行选择身份登录。
- **根因**：用户角色应由数据库决定，前端角色选择只是一项没有业务效果的界面状态。
- **解决方案**：移除登录角色选择器，登录后按照服务端返回的角色和制片厂状态自动分流。
- **验证方式**：管理员、审核员、普通用户和制片厂使用同一登录页，并进入各自页面。
- **后续改进**：所有权限判断必须由后端执行，前端只负责展示和导航。

## 5. 注册接口存在但前端没有注册入口

- **日期**：2026-06-12
- **现象**：后端已有 `/auth/register`，但用户无法从页面注册。
- **影响**：普通用户和制片厂账号无法自行创建。
- **根因**：前后端功能没有完整接通。
- **解决方案**：将登录与注册整合在统一认证页；公开注册只允许 `USER` 和 `STUDIO`，注册成功后切换回登录模式。
- **验证方式**：注册表单可选择普通用户或制片厂，并显示“注册成功，请登录”。
- **后续改进**：增加邮箱验证、密码强度提示和找回密码功能。

## 6. `/auth/me` 被公开放行

- **日期**：2026-06-12
- **现象**：安全配置使用 `/auth/**` 全部放行，导致当前用户接口也不要求认证。
- **影响**：认证边界不清晰，容易出现无效 token 被错误处理的问题。
- **根因**：公开接口匹配范围过大。
- **解决方案**：只放行 `/auth/login` 和 `/auth/register`，`/auth/me` 必须经过 JWT 认证。
- **验证方式**：匿名访问 `/api/auth/me` 返回 HTTP 401。
- **后续改进**：为所有敏感接口补充权限集成测试。

## 7. 全局异常处理缺失

- **日期**：2026-06-12
- **现象**：业务异常可能返回 500 或不同格式的错误响应。
- **影响**：前端无法稳定展示错误信息，也无法区分 400、401、403 和 409。
- **根因**：原全局异常处理类为空，业务代码大量抛出通用异常。
- **解决方案**：增加 `ApiException` 和全局异常处理器，统一返回 `ApiResponse`；参数错误、认证失败、权限不足和数据冲突使用正确状态码。
- **验证方式**：相关单元测试通过，未登录访问返回 JSON 401。
- **后续改进**：增加错误编码、请求 ID 和结构化错误日志。

## 8. 受保护路由只信任 localStorage

- **日期**：2026-06-12
- **现象**：只要本地存储存在 token 和用户 JSON，前端就允许进入受保护路由。
- **影响**：过期或伪造的本地数据可能暂时显示无权限页面。
- **根因**：路由守卫没有向服务端验证 token。
- **解决方案**：受保护路由进入时调用 `/auth/me`，根据服务端返回的真实角色和状态决定跳转。
- **验证方式**：未登录访问 `/studio` 会跳转到 `/login`。
- **后续改进**：引入统一认证上下文，减少页面重复读取 localStorage。

## 9. 制片厂注册与申请流程混在一起

- **日期**：2026-06-12
- **现象**：制片厂可能被要求注册时立即提交完整资料。
- **影响**：不符合制片厂可以稍后准备片库和申请材料的业务需求。
- **根因**：账号注册和资质申请没有拆分。
- **解决方案**：制片厂注册后状态为 `NONE`；登录后进入申请页，可稍后提交；`PENDING` 等待审核，`REJECTED` 可修改后重新提交，`APPROVED` 才能进入工作台。
- **验证方式**：制片厂申请状态单元测试通过。
- **后续改进**：记录审核意见、申请历史和审核操作日志。

## 10. 单条 Video 模型无法管理电视剧和综艺

- **日期**：2026-06-12
- **现象**：电视剧和综艺的每一集都需要重复填写整部作品的信息。
- **影响**：数据重复，季度和剧集难以排序、更新和审核。
- **根因**：原模型只有单层 `Video`。
- **解决方案**：新增 `Content`、`Season` 和 `Episode` 模型；电影使用一个无季度剧集，电视剧和综艺通过季度组织多集。
- **验证方式**：作品、季度和剧集服务测试通过，制片厂工作台可分别创建。
- **后续改进**：增加批量导入、文件上传、转码状态和旧视频数据迁移。

## 11. 游客试看缺少业务规则

- **日期**：2026-06-12
- **现象**：公开首页与完整视频观看没有区分。
- **影响**：游客访问策略不明确。
- **根因**：播放器没有根据认证状态限制播放时长。
- **解决方案**：每个剧集默认 `previewSeconds=300`；游客播放到五分钟时自动暂停并提示登录或注册。
- **验证方式**：公开首页可匿名访问，播放器组件按剧集试看时间执行限制。
- **后续改进**：视频源改为签名 HLS 地址，避免用户直接绕过前端访问公开 URL。

## 12. 缺少默认管理员账号

- **日期**：2026-06-12
- **现象**：系统启动后没有可用于进入管理后台的账号。
- **影响**：无法审核制片厂、管理用户和创建审核员。
- **根因**：公开注册禁止管理员，但没有开发环境初始化账号。
- **解决方案**：增加仅在 `dev` profile 生效的 `DevAdminInitializer`，管理员不存在时使用 BCrypt 创建默认账号，存在时不覆盖。
- **验证方式**：使用 `admin / Admin@123456` 调用真实登录接口成功。
- **后续改进**：生产环境使用部署密钥或一次性初始化流程，不使用固定默认密码。

## 13. 管理员控制台不能创建审核员

- **日期**：2026-06-12
- **现象**：后端存在用户管理接口，但管理员控制台没有创建账号和调整角色的入口。
- **影响**：审核员账号无法正常获得。
- **根因**：管理业务只完成了后端部分，没有完整前端流程。
- **解决方案**：管理员控制台增加创建账号弹窗和角色调整控件；支持创建普通用户、制片厂和审核员。禁止直接创建管理员，禁止管理员降级自己，并保证至少保留一个管理员。
- **验证方式**：后端角色规则测试通过；浏览器中可看到创建审核员表单和角色管理表格。
- **后续改进**：增加禁用账号、重置初始密码和操作审计。

## 14. 端口 8080 被重复启动的后端占用

- **日期**：2026-06-12
- **现象**：IDEA 启动时报错 `Port 8080 was already in use`。
- **影响**：新的 Spring Boot 进程无法启动。
- **根因**：功能验证时后台启动的 `VideoAiApplication` 仍在监听 8080；2026-06-15 再次出现时，监听进程显示为 Docker Desktop 的 `com.docker.backend.exe`，进一步检查确认实际占用者是映射 `0.0.0.0:8080->8080` 的本项目 `video-ai-backend` 容器。
- **解决方案**：先通过端口检查获取 PID 并核对进程命令行。若监听者是 Docker Desktop，不直接结束 Docker 后台，而是使用 `docker ps` 定位发布 8080 的具体容器。需要同时保留两个后端时，Docker 后端继续映射宿主机 8080，`application-dev.yml` 将 IDEA 启动的开发后端改为 8081，Vite 的 `/api` 开发代理同步指向 `http://localhost:8081`。
- **验证方式**：静态配置检查确认 Docker 保留 `8080:8080`、开发 profile 使用 8081、Vite 代理指向 8081。Docker 的 `http://localhost:8080/api/contents/public` 返回 HTTP 200；使用与 IDEA 相同的 `dev` profile 临时启动本地后端后，日志显示 Tomcat 在 8081 启动，`http://localhost:8081/api/contents/public` 返回 HTTP 200；通过 Vite 的 `http://localhost:3000/api/contents/public` 访问同样返回 HTTP 200。验证结束后停止临时进程，确认 8081 已释放给 IDEA。当前 Actuator health 接口因安全配置返回 HTTP 401，不作为匿名可用性检查。
- **后续改进**：启动失败时先检查端口与容器映射，避免直接结束不明进程；Docker 后端用于容器化验证，IDEA 后端用于本地调试，前端开发代理固定连接 IDEA 的 8081。

## 15. 项目仍以 CRUD 为主，缺少生产级视频能力

- **日期**：2026-06-12
- **现象**：目前没有视频转码、高并发播放、防盗链、搜索引擎、单设备登录和完整互动数据链路。
- **影响**：项目业务复杂度、系统设计和生产就绪程度仍然有限。
- **根因**：当前阶段优先完成了认证、角色、内容建模和基础业务闭环。
- **解决思路**：
  - 使用 MinIO/S3 保存原始视频和转码结果。
  - 使用 FFmpeg 将视频转为多码率 HLS。
  - 使用消息队列异步执行转码、重试和搜索索引更新。
  - 使用 CDN 与签名 URL 分发视频并防盗链。
  - 使用 Redis 管理单设备会话、限流、热门缓存、播放进度缓冲和点赞计数。
  - 使用独立的播放进度、点赞、收藏和评论表，唯一索引保证幂等。
  - 小规模搜索使用 PostgreSQL 全文检索和 `pg_trgm`，数据增长后迁移到 Elasticsearch/OpenSearch。
  - 使用 Actuator、Prometheus 和 Grafana 建立监控。
- **验证方式**：后续每个阶段分别增加性能测试、故障测试和接口集成测试。
- **后续改进**：建议下一阶段优先实现 Redis 单设备登录、播放进度、点赞评论、限流和监控，再实现转码与对象存储。

## 16. AdminDashboard 和 Login 被 ESLint 配置错误连带标红

- **日期**：2026-06-12
- **现象**：`AdminDashboard.tsx` 和 `Login.tsx` 在编辑器中报错，执行 `npm run lint` 时 ESLint 在加载配置阶段直接崩溃。
- **影响**：无法获得页面代码的真实静态检查结果，页面中的有效错误被配置异常遮挡。
- **根因**：项目安装的 `eslint-plugin-react-hooks` 提供 `recommended-latest` 配置，但 `eslint.config.js` 错误引用了不存在的 `reactHooks.configs.flat.recommended`；配置修复后，两个页面还各有一处丢弃 `confirmPassword` 时产生的未使用变量。
- **解决方案**：将 Hooks 配置改为 `reactHooks.configs['recommended-latest']`；登录注册和管理员创建账号时显式构造 API 请求对象，不再通过未使用的解构变量移除确认密码。
- **验证方式**：两个页面的定向 ESLint 检查通过；`npm run build` 成功；登录与注册界面在浏览器中正常切换且控制台无错误。
- **后续改进**：继续处理其他页面现有的 `no-explicit-any` 和 Hooks 依赖警告，使全项目 `npm run lint` 完整通过。

## 17. 本地 PostgreSQL 未启动导致后端无法联调

- **日期**：2026-06-12
- **现象**：启动 Spring Boot 时提示无法连接 `localhost:5432`，后端未能监听 8080。
- **影响**：登录接口和管理员控制台的数据接口无法进行浏览器端到端验证。
- **根因**：本机 PostgreSQL 服务当前没有监听 5432 端口。
- **解决方案**：先启动 PostgreSQL 服务或项目的数据库容器，确认 `video_db` 可连接后再启动后端。
- **验证方式**：使用端口检查确认 5432 已监听，再访问 `/api/actuator/health` 或执行管理员登录验证。
- **后续改进**：在开发启动脚本中加入数据库可用性检查，并为缺失依赖提供清晰提示。

## 18. 退出后首页仍显示上一次登录用户

- **日期**：2026-06-12
- **现象**：审核员张三退出并回到登录页后，重新启动程序访问 `localhost:3000`，首页仍显示“欢迎，张三”。
- **影响**：游客会被错误展示为已登录状态，试看限制也会因为缓存用户而被绕过。
- **根因**：`localStorage` 会跨浏览器和程序重启持久保存；首页只读取缓存中的 `user`，没有检查 `token` 是否存在，也没有调用 `/auth/me` 验证登录状态。
- **解决方案**：新增统一认证会话模块。首页初始化时，没有 token 就清除残留用户；有 token 则调用 `/auth/me`，只使用服务端验证后的用户；验证失败时清空 token 和用户缓存。Axios 的 401 清理和主动退出也统一使用同一清理函数。
- **验证方式**：增加 3 个认证会话回归测试，覆盖无 token 残留用户、有效 token 和校验失败场景；定向 ESLint 与生产构建通过。
- **后续改进**：后续可将用户状态提升到统一的 React Auth Context，并通过 `storage` 事件同步多个浏览器标签页的登录和退出状态。

## 19. 前端可运行但 IDE 和 ESLint 持续爆红

- **日期**：2026-06-12
- **现象**：开发服务器可以正常运行，但 `StudioReviewerDashboard.tsx` 存在 6 个 `any` 错误和 Hook 依赖警告，`StudioWorkspace.tsx` 也有 Hook 依赖警告。
- **影响**：开发模式没有阻止页面启动，但缺失类型会掩盖接口结构变化，Hook 闭包可能读取旧状态，IDE 持续显示错误。
- **根因**：旧视频 API 没有声明请求和响应类型，页面直接处理 Axios 原始响应并大量使用 `any`；两个异步加载函数每次渲染都会创建新引用，却没有正确声明 Hook 依赖。
- **解决方案**：补充视频、创建视频和审核视频的 TypeScript 类型；API 层统一解包业务数据；审核员页面使用 `UserInfo` 和 `/auth/me`；表格回调使用 `unknown`；异步加载函数使用 `useCallback` 并补全依赖。
- **验证方式**：执行 `npm run lint` 无错误和警告；前端测试与生产构建通过。
- **后续改进**：逐步将后端 DTO 通过 OpenAPI 自动生成前端类型，避免接口结构变化后各页面手工同步。

## 20. 单设备登录失效后跳转登录页但没有提示

- **日期**：2026-06-12
- **现象**：账号在其他设备重新登录后，旧设备刷新会正确跳转到登录页，但看不到“您的账号已在其他设备登录，请重新登录”。
- **影响**：用户不知道自己为什么退出，容易误以为登录过期或系统故障。
- **根因**：最初拦截器在整页跳转前显示消息，页面卸载导致消息消失；改为 `sessionStorage` 后仍未生效，是因为 `JwtAuthenticationFilter` 没有设置 UTF-8，真实响应变成 `{"message":"??????????????????"}`，前端无法识别中文。另外，React 开发模式的 `StrictMode` 会重复执行挂载流程，“读取即删除”可能在第一次模拟挂载时提前消费提示。
- **解决方案**：过滤器明确返回 UTF-8，并增加稳定响应头 `X-Auth-Reason: SESSION_REPLACED`；前端优先按原因代码识别，中文文本仅作兼容回退。登录页改为先读取但不立即删除，使用固定消息 key 显示，并在挂载稳定后延迟清除提示代码。
- **验证方式**：后端过滤器测试验证 401、UTF-8、原因代码和完整中文；前端认证提示测试覆盖保存、读取、清除、单次消费和原因识别；真实执行两个客户端依次登录同一管理员账号后，旧客户端刷新跳转 `/login`，浏览器检测到目标提示恰好一条。
- **后续改进**：可将认证失效原因统一定义为后端错误码，避免前端依赖中文消息文本进行分类。

## 21. 同一浏览器中的两个客户端会互相覆盖和注销会话

- **日期**：2026-06-12
- **现象**：第一个客户端登录管理员后，第二个客户端登录同一账号会收到“账号已在其他设备登录”；第一个客户端退出后，第二个客户端仍无法继续使用。
- **影响**：新登录客户端会被旧客户端的请求或退出操作误伤，不符合“新登录会话有效、旧会话失效”的单设备登录规则。
- **根因**：前端把 JWT 放在同源共享的 `localStorage`。第二个标签页登录后会覆盖第一个标签页读取到的 token，第一个标签页退出时实际提交的是第二个标签页的新 token。并且旧请求稍后返回的 401 会无条件清空刚写入的新 token。后端退出接口也只按 `userId` 删除 Redis 会话，缺少 `sessionId` 匹配保护。
- **解决方案**：认证 token 和用户信息统一改存每个标签页独立的 `sessionStorage`；Axios 收到 401 时比较该请求携带的 token 与当前 token，旧请求响应不得清理新会话；退出接口从 JWT 提取 `userId` 与 `sessionId`，仅在 Redis 当前 session 匹配时删除。
- **验证方式**：前端回归测试覆盖旧 token 的 401 不清理新 token；后端回归测试覆盖旧 session 不能删除当前 session、当前 session 可以正常退出；使用两个 token 依次登录同一账号，验证旧 token 返回 401、新 token 返回 200、旧 token 退出不影响新 token。
- **后续改进**：将 Redis 的“比较 sessionId 后删除”升级为 Lua 脚本原子操作，并增加真实 Redis 的并发集成测试。

## 22. 审核接口返回成功但数据库状态仍为 PENDING

- **日期**：2026-06-14
- **现象**：在 `ReviewDetail` 中提交“审核通过”后，接口返回成功，`reviewed_by`、`reviewed_at` 和 `review_comment` 已写入 `contents` 表，但同一行的 `status` 一度仍为 `PENDING`。
- **影响**：作品无法进入“已通过、待发布”阶段；审核员重复进入详情页时仍能再次审核，容易误判为整个审核请求没有落库。
- **排查证据**：后端日志确认 `PATCH /api/contents/2/review` 已映射到 `ContentController#reviewContent`，请求体中的状态为 `APPROVED`，Hibernate 也执行了 `UPDATE contents`。直接查询数据库确认审核人、审核时间和备注已变化，但最初状态未变化。重新编译并加载当前后端代码后，使用真实 JWT 进行端到端调用，响应和数据库中的状态均正确变为 `APPROVED`。
- **根因**：运行中的后端类与正在查看、修改的源码版本曾不一致，导致早期请求没有完整执行当前 `reviewContent` 的状态保存逻辑。重复调用 `saveAndFlush(content)` 后又调用 `save(content)` 也增加了排查噪声，但不是必要操作。
- **解决方案**：确认运行进程加载的是当前 `target/classes`；审核方法设置 `status`、审核人、审核时间和备注后，只调用一次 `contentRepository.saveAndFlush(content)`，并使用返回的已保存实体生成 DTO。
- **验证方式**：真实调用审核接口后立即查询 `contents` 表，确认 `status=APPROVED`；新增服务测试验证返回状态和审核字段，并验证调用了 `saveAndFlush`；执行后端全部 53 个测试，全部通过。
- **后续改进**：调试“代码看起来正确但行为不一致”时，应同时核对源码修改时间、编译产物修改时间、Java 进程启动时间和实际 classpath。不要仅靠 `saveAndFlush` 猜测事务问题，先用日志与数据库查询确认每一层的数据。

## 23. ReviewerDashboard 将审核状态硬编码为“待审核”

- **日期**：2026-06-14
- **现象**：审核完成并返回 `ReviewerDashboard` 后，列表中的审核状态始终显示“待审核”；即使接口和数据库已经返回 `APPROVED`，页面仍没有视觉变化。
- **影响**：用户误以为审核没有成功，也看不到审核通过后的“发布”动作。
- **根因**：状态列使用了固定渲染函数 `render: () => <Tag>待审核</Tag>`，完全没有读取 `record.status`。同时，后端审核队列按业务需要同时返回 `PENDING` 和 `APPROVED`，所以审核通过的作品仍会留在列表中等待发布；固定标签把这一正常流程伪装成了状态未更新。
- **解决方案**：新增统一的审核状态展示映射，根据真实 `VideoStatus` 返回标签、颜色和是否允许发布；状态列读取 `record.status`；`APPROVED` 行显示“已通过”和“发布”按钮，发布按钮阻止行点击冒泡并在发布后重新加载列表。
- **验证方式**：新增前端回归测试，覆盖 `PENDING -> 待审核` 和 `APPROVED -> 已通过且可发布`；前端全部 23 个测试通过，相关文件 ESLint 通过。完整前端构建仍被既有的 `AdminDashboard.tsx:51` 未使用常量阻塞，与本次审核修复无关。
- **后续改进**：列表中的状态、权限和可用动作必须来源于服务端返回的数据，禁止使用固定文案模拟业务状态。遇到“页面没变化”时，应依次检查网络响应、数据库记录、列表重新加载和渲染映射。

## 24. Computer Use 插件运行时无法初始化

- **日期**：2026-06-14
- **现象**：尝试通过 Computer Use 对审核页面进行桌面视觉验证时，初始化阶段报错：`@oai/sky` 的内部模块路径未通过包的 `exports` 导出。
- **影响**：本次无法通过 Windows 桌面自动化实际点击并截图验证页面，只能依靠接口、数据库、单元测试、类型检查和静态代码检查完成验证。
- **根因**：Computer Use 插件与当前本地 `@oai/sky` 运行时版本不兼容，属于工具运行环境问题，不是项目代码问题。
- **解决方案**：停止继续发送桌面输入，保留已完成的代码和接口验证，并明确记录视觉验证未执行及具体初始化错误。
- **验证方式**：Computer Use 引导脚本在首次轻量连接前即稳定复现相同的模块导出错误；项目自身的前后端测试不受影响。
- **后续改进**：插件运行时更新后重新进行浏览器或桌面视觉验收；工具不可用时不得把静态检查描述为已经完成的人工或视觉验收。

## 25. 已通过作品点击发布时返回 400

- **日期**：2026-06-14
- **现象**：`ReviewerDashboard` 中显示“发布”按钮，但点击后 `PATCH /api/contents/2/publish` 返回 HTTP 400，后端消息为 `Only approved content can be published`。
- **影响**：审核员无法完成发布，并容易误以为发布服务或剧集数据损坏。
- **排查证据**：后端日志显示发布请求发生于 17:14:04，当时数据库中的作品状态是 `PENDING`；同一作品在 17:16:04 重新审核后才变为 `APPROVED`。作品包含一集有效视频，不是“缺少剧集”导致的失败。
- **根因**：上一次调试审核流程时，为恢复测试现场直接把真实业务记录改回了 `PENDING`，但浏览器中的旧列表仍可能保留之前的 `APPROVED` 行数据和发布按钮。后端以数据库最新状态为准，因此正确拒绝了陈旧页面发起的发布请求。
- **解决方案**：不再修改真实业务记录来清理调试现场；重新审核后完成实际发布。前端发布前重新调用审核队列接口，只有服务端最新记录仍为 `APPROVED` 才提交发布；否则刷新列表并提示状态已变化。发布期间只将当前行按钮置为加载状态。
- **验证方式**：第一次重新审核后，发布接口虽然返回 `PUBLISHED`，但数据库复查发现作品仍为 `PENDING`，因此没有把接口响应误当成最终成功，而是继续按第 27 条排查持久化问题。修复后再次完成真实审核与发布，数据库和公开接口才同时确认作品已发布。新增前端测试验证刷新后的 `PENDING`、记录缺失均不可发布，只有 `APPROVED` 可以发布。
- **后续改进**：端到端调试应使用专用测试数据或事务回滚，禁止为了“恢复现场”直接覆盖用户正在操作的真实业务记录。所有状态变更按钮都应在提交前校验服务端最新状态。

## 26. Ant Design 静态消息和 Select 弃用属性警告

- **日期**：2026-06-14
- **现象**：控制台出现 `[antd: message] Static function can not consume context like dynamic theme`，以及 `Select popupClassName is deprecated`。
- **影响**：消息组件不能可靠继承动态主题上下文；后续升级 Ant Design 时旧的下拉框样式属性可能被移除。
- **根因**：`ReviewDetail` 和 `StudioReviewerDashboard` 直接调用静态 `message.*`；`ReviewDetail` 仍使用旧的 `popupClassName` 属性。
- **解决方案**：页面改为通过 `App.useApp()` 获取上下文消息实例；Select 改用 `classNames={{ popup: { root: 'review-dropdown-custom' } }}`。
- **验证方式**：相关前端测试、TypeScript 构建和定向 ESLint 检查通过。
- **后续改进**：新增页面统一使用项目根部已经配置的 Ant Design `App` 上下文，避免继续引入静态消息 API 和已弃用组件属性。

## 27. 发布接口返回 PUBLISHED 但数据库作品状态仍为 PENDING

- **日期**：2026-06-14
- **现象**：作品重新审核为 `APPROVED` 后，`PATCH /api/contents/2/publish` 返回 HTTP 200，响应 DTO 的状态也是 `PUBLISHED`，剧集 `published_at` 已写入；但随后直接查询 `contents` 表，作品状态仍为 `PENDING`，公开内容接口也查不到该作品。
- **影响**：前端会提示“作品已发布”，实际数据库却没有完成作品状态迁移，造成内存状态、接口响应和持久化状态互相矛盾。
- **排查证据**：发布事务日志显示同一请求执行了 `UPDATE contents` 和 `UPDATE episodes`，响应对象为 `PUBLISHED`；数据库行的 `updated_at` 与该事务时间一致，但状态仍错误。没有后续请求、数据库触发器或状态重置逻辑能够解释这次回退，因此必须在发布方法内部把作品状态的落库作为明确的事务边界验证。
- **根因**：发布方法设置实体状态后只调用普通 `contentRepository.save(content)`，随即继续组装包含关联查询的 DTO。该路径只证明托管实体在内存中已变为 `PUBLISHED`，没有为关键状态迁移建立立即 flush 的持久化边界，因而接口成功响应无法作为数据库已更新的证据。
- **解决方案**：发布方法在设置 `content.status=PUBLISHED` 后改用 `contentRepository.saveAndFlush(content)`，使用其返回实体生成 DTO；剧集发布时间与作品状态仍处于同一事务中，任一落库失败都会使请求失败并回滚。新增回归测试明确验证发布路径调用 `saveAndFlush`。
- **验证方式**：修复后使用当前审核员真实会话依次调用审核和发布接口，响应状态分别为 `APPROVED`、`PUBLISHED`；随后独立查询 PostgreSQL，确认 `contents.id=2` 为 `PUBLISHED`、对应剧集 `published_at` 已刷新；匿名调用 `/api/contents/public` 可以查到该作品。前端 24 个测试、定向 ESLint 和生产构建通过，后端发布回归测试通过。
- **后续改进**：涉及审核、发布、支付等关键状态迁移时，测试不能只断言返回 DTO，必须同时约束持久化调用，并优先增加使用隔离测试数据库的仓储或接口集成测试。端到端验收要同时检查 HTTP 响应、数据库最终状态和下游查询结果。

## 28. 后端重编译重启窗口内旧实例仍接收审核请求

- **日期**：2026-06-14
- **现象**：源码已经改为审核、发布后立即落库，但页面再次审核后数据库仍出现 `PENDING`；随后点击发布继续返回 400。用户怀疑旧 `videos` 数据干扰新内容模型。
- **影响**：同一套页面操作在不同时间得到不同结果，容易把运行实例问题误判为旧表数据冲突或数据库缓存。
- **排查证据**：`videos` 表实际记录数为 0；审核与发布接口只使用 `ContentRepository`、`contents` 和 `episodes`，没有调用 `VideoRepository`；`contents` 表没有数据库触发器。日志显示异常审核发生于 17:46:22，而 DevTools 在 17:46:26 开始重新加载 classpath，新实例直到 17:47:19 才启动完成，因此该请求仍由旧运行实例处理。新实例完成后，Hibernate 参数日志明确显示审核状态绑定为 `APPROVED`。
- **根因**：开发环境运行着 Spring Boot DevTools。Maven 编译会分阶段删除、重新生成 `target/classes`，触发一次或多次重启；在新实例完全启动之前，旧实例仍可能短暂接收页面请求。此前实体 `save/merge` 路径又允许内存 DTO 与数据库最终状态不一致，使这个时间窗口更难识别。
- **解决方案**：不删除旧 `videos` 数据。审核和发布改为仓储层显式条件更新：审核只允许 `PENDING -> APPROVED/REJECTED`，发布只允许 `APPROVED -> PUBLISHED`；更新行数必须等于 1，否则返回冲突错误；更新后清理持久化上下文并重新查询数据库实体生成 DTO。开发期间重新编译后，等待日志出现最新一次 `Started VideoAiApplication` 再操作页面。
- **验证方式**：新实例启动完成后，真实调用审核接口，响应与数据库均为 `APPROVED`；紧接着调用发布接口，响应与数据库均为 `PUBLISHED`。旧 `videos` 表保持为空，未执行删除操作。
- **后续改进**：不要在正在运行 DevTools 的同一 `target/classes` 上并发执行全量 Maven 编译和业务验收；可在测试时停止开发服务器，或为测试使用独立构建目录和隔离数据库。页面可增加后端启动版本或构建时间展示，便于确认当前连接的实例。

## 29. pgAdmin 无法解析 PostgreSQL 主机名

- **日期**：2026-06-15
- **现象**：pgAdmin 连接数据库时报错 `failed to resolve host 'postgres': [Errno -2] Name does not resolve`。
- **影响**：虽然 PostgreSQL 容器健康且宿主机 5432 可访问，但 pgAdmin 无法建立数据库连接。
- **根因**：PostgreSQL 容器位于 `video_ai_video-network`，pgAdmin 容器仅位于 `video_ai_video-network-dev`；Docker 服务名 `postgres` 只能在共享网络中解析，密码不是本次错误的原因。
- **解决方案**：将 pgAdmin 同时接入开发管理网络和 `video_ai_video-network`，保留连接主机名 `postgres`。数据库参数为数据库 `video_db`、用户 `postgres`、端口 `5432`；密码沿用 Compose 配置。
- **验证方式**：在 PostgreSQL 容器内使用配置密码执行 `select current_database(), current_user`，返回 `video_db|postgres`；从 pgAdmin 容器解析 `postgres` 并连接 5432。
- **后续改进**：同时运行主 Compose 与开发管理工具时，所有依赖 Docker 服务名通信的容器必须共享目标服务所在网络；不要把名称解析错误误判为密码错误。

## 30. 播放页依赖不存在的详情接口且错误导入 DPlayer 样式

- **日期**：2026-06-15
- **现象**：原 `VideoPlayPage` 调用前端未定义的 `contentApi.getEpisodeDetails`、`contentApi.getById`，并假设 `Episode` 包含 `contentId/content`；接入 DPlayer 后，构建又无法解析 `dplayer/dist/DPlayer.min.css`。
- **影响**：播放页无法通过 TypeScript 构建，也无法从首页稳定跳转或在刷新后恢复播放数据。
- **根因**：播放页的数据契约先于真实 API 编写，前后端没有对应的公开剧集详情接口；DPlayer 1.27.1 的 npm 发布包只有 `dist/DPlayer.min.js` 和 source map，样式已经包含在打包产物中，没有独立 CSS 文件。
- **解决方案**：播放路由改为 `/play/:contentId/:episodeId`，复用现有 `/contents/public` 数据严格解析作品与剧集；抽取纯函数模型统一选集排序和路由构造；DPlayer React 封装只导入 `dplayer`，不再导入不存在的 CSS，并在剧集切换或卸载时销毁实例。
- **验证方式**：新增 4 个播放模型测试并先确认因模型缺失而失败，补充实现后通过；前端完整 28 个测试通过，生产构建成功；浏览器验证首页作品卡片进入 `/play/1/1`、DPlayer 控件可见、390px 视口无横向溢出、无效剧集 `/play/1/999` 显示明确错误状态。
- **后续改进**：后端若增加公开作品详情接口，可将播放页从全量公开列表查询迁移到按作品 ID 加载；接入真实弹幕、多码率 HLS 和推荐服务时继续保持播放器封装边界。

## 31. 4:3 视频在 16:9 播放器中出现黑边且播放页背景割裂

- **日期**：2026-06-15
- **现象**：播放页使用固定 16:9 播放器时，较窄的视频画面左右出现纯黑留边；页面其余区域也是平面黑色，与首页的赛博黑、蓝紫流光视觉不统一。
- **影响**：用户容易把正常的画幅适配误认为播放器没有全屏，同时无法按观看偏好选择保留原画面、拉伸铺满或裁剪铺满。
- **排查证据**：浏览器读取当前视频原始尺寸为 `721×528`，比例约为 1.37，接近 4:3；播放器容器固定为 16:9，DPlayer 视频元素默认计算样式为 `object-fit: contain`，因此左右留边是浏览器保持原比例后的预期结果，不是播放器宽度失效。
- **根因**：播放器只提供默认的等比例缩放，没有暴露画面比例切换；播放器留白区域和播放页外层又使用纯黑或不透明深色背景，未复用项目已有的 `linear-bg` 分层背景系统。
- **解决方案**：新增独立画面设置控件，提供 `contain` 等比例缩放、`fill` 16:9 拉伸铺满和 `cover` 居中智能裁剪三种模式；模式状态独立于 DPlayer 初始化 effect，切换时只更新现有 video 元素样式。播放页复用现有 7 层赛博流光背景，并将头部、评分、评论、选集和推荐区域改为半透明深色表面；播放器留白区域使用克制的蓝紫深色渐变。
- **验证方式**：画面模式测试先因模型文件缺失而失败，补充实现后通过；前端完整 31 个测试通过，生产构建成功，ESLint 无新增错误。浏览器确认默认、拉伸、裁剪模式的计算样式分别为 `contain`、`fill`、`cover`，Enter 可打开画面菜单；390px 视口无横向溢出，画面按钮为 44×36，背景包含 7 个既有层。自动化浏览器未能启动媒体播放，仅确认切换时暂停状态和 `currentTime=0` 未变化，播放中进度保持仍需人工观看时复核。
- **后续改进**：后续可将用户选择写入账户偏好或本地存储；若接入不同画幅和多码率视频，应增加真实播放中的进度、全屏状态和剧集切换回归测试。

## 32. 首页频道只保存在组件状态导致刷新和分享链接失真

- **日期**：2026-06-15
- **现象**：首页 Header 中切换电视剧、电影或综艺时，只改变当前页面显示内容，地址栏仍停留在 `/`；刷新后恢复为首页混合内容，无法直接分享某个频道。播放页虽然使用 `/?type=...` 返回分类，但首页的 `activeTab` 又只在组件初始化时读取一次查询参数。
- **影响**：频道不是稳定的页面入口，刷新、前进后退和链接分享的结果不一致；后续增加 genre 筛选时也缺少可持久化的频道上下文。
- **根因**：频道选择由 `ConsumerHome` 内部 `activeTab` 状态控制，Ant Design Tabs 的 `onChange` 只调用 `setActiveTab`，没有形成路由契约；频道分组、类型标签和播放页跳转也分别维护，存在重复映射。
- **解决方案**：增加 `/`、`/tv`、`/movies`、`/variety` 四条明确路由，由 `ConsumerHome` 的 `channel` 属性决定页面模式；抽取纯 `consumerChannelModel` 统一频道路径、内容分组、类型过滤、genre 顺序与参数校验。频道页使用 `?genre=` 保存题材选择，无效参数自动清除；播放页 Header 复用相同频道路由。
- **验证方式**：频道模型测试先因实现文件缺失而失败，补充实现后通过；前端完整 35 个测试通过，生产构建成功，ESLint 无新增错误。浏览器直接刷新四条路由均显示正确的活动导航；`/tv` 只显示电视剧，`/tv?genre=ANIMATION` 选中动画，`?genre=UNKNOWN` 自动归一化为 `/tv`，有效但无内容的 `?genre=ACTION` 显示分类空状态。390px 视口无页面横向溢出，频道导航可横向滚动，genre 按钮高度为 44px。
- **后续改进**：公开内容数量增长后，可将频道和 genre 参数传给后端分页接口，避免一次加载完整目录；频道元数据应继续保持单一来源，不再在页面内复制路径和标签。
## 33. 播放页评分评论推荐依赖前端拟真数据

- **日期**：2026-06-16
- **现象**：播放页通过 `buildPlaybackDemo` 在前端生成评分人数、评分值、评论作者、评论内容和同类推荐展示，后端的 `CommentController`、`CommentService`、`CommentRepository`、`Comment` 仍是空壳。
- **影响**：用户看到的互动数据无法持久化，也无法形成登录用户评分、发表评论、刷新后保留评论、按真实作品推荐的闭环。
- **根因**：播放页先完成了视觉展示，但评论、评分和推荐没有落到当前 `Content + Episode` 后端模型上，旧 `Video` 字段也未被播放页使用。
- **解决方案**：新增 `content_comments`、`content_ratings` 持久化模型和 `/contents/{contentId}/comments`、`/contents/{contentId}/rating`、`/contents/{contentId}/recommendations` 接口；前端删除 `buildPlaybackDemo`，播放页改为加载真实评论、评分摘要和后端推荐，登录后可提交评分与评论。
- **验证方式**：先新增后端评论、评分、推荐测试并确认因缺少 DTO/实体/仓库失败；实现后 `.\mvnw "-Dtest=ContentEngagementServiceTest,ContentEngagementControllerTest" test` 通过。前端先新增“不得导出 `buildPlaybackDemo`”测试并确认失败；删除拟真数据后 `npm test -- --run src/pages/videoPlayModel.test.ts` 通过，`npm run build` 通过。
- **后续改进**：可继续增加评论删除/举报、评分分布、推荐分页、基于播放历史的个性化推荐，以及后端集成测试覆盖真实数据库约束。

## 34. 游客进入播放页被互动接口 401 重定向到登录

- **日期**：2026-06-16
- **现象**：游客在首页点击已发布作品后，没有进入试看播放页，而是被直接跳转到登录页。
- **影响**：游客试看链路被阻断，播放组件内的试看时长限制无法触发。
- **根因**：播放页路由和首页作品卡片跳转本身都是公开的，`DPlayerPlayer` 也支持游客试看；但播放页加载新增的评论、评分和推荐 GET 接口时，后端安全配置没有放行 `/contents/{contentId}/comments`、`/contents/{contentId}/rating`、`/contents/{contentId}/recommendations`，这些请求返回 401 后被 Axios 全局拦截器重定向到 `/login`。
- **解决方案**：在 `SecurityConfig` 中显式放行三个播放页互动读取接口的 GET 请求，保留发表评论和提交评分的 POST 请求必须登录。
- **验证方式**：新增 `SecurityConfigTest` 覆盖匿名读取播放页互动数据；执行 `.\mvnw test`，65 个后端测试全部通过。
- **后续改进**：可补充更完整的安全集成测试，验证匿名 GET 返回 200 且匿名 POST 仍返回 401/403；前端也可对播放页辅助接口的 401 做局部错误提示，避免把辅助数据失败误导为整个播放页需要登录。

## 35. Vite 生产构建出现大 chunk 警告

- **日期**：2026-06-17
- **现象**：执行 `npm run build` 时，Vite 提示部分 chunk 超过 500 KiB；优化前主入口 JS 约 1,688 KiB。
- **影响**：不阻断构建和运行，但会让首次加载下载、解析和执行成本集中在首屏入口，尤其影响弱网和移动端体验。
- **根因**：`App.tsx` 静态导入所有路由页面，登录页、首页、播放页、后台、审核页和创作者工作台被一起打进入口；同时 React、DPlayer、Axios 等依赖缺少清晰的 vendor 分包边界。
- **解决方案**：将路由页面改为 `React.lazy` 加 `Suspense` 懒加载；在 `vite.config.ts` 中为 React、播放器、Axios 和其他稳定依赖设置 `manualChunks`，并让 Ant Design 由 Rollup 按实际路由共享关系自动拆分，避免过度手动拆分导致循环 chunk。
- **验证方式**：先新增 `scripts/assert-build-chunks.mjs` 并确认优化前能检出 `index-CJBdg-5m.js 1658.3 KiB`；优化后执行 `npm run build` 无 Vite 大 chunk 警告，最大 JS chunk 为 `vendor-player-kPPMVorT.js 297.94 KiB`，检查脚本输出 `All 48 JS chunks are under 500 KiB.`；`npm run test` 通过 9 个测试文件、38 条测试；`npm run lint` 仍为 0 个错误，仅保留既有 `UserProfile.tsx` Hook 依赖警告。
- **后续改进**：后续可继续把重型 Ant Design 页面组件改为更细粒度的局部懒加载，或按真实访问路径预加载常用页面，进一步改善首屏和路由切换体验。

## 36. 评论头像上传后没有在播放页刷新显示

- **日期**：2026-06-17
- **现象**：用户发布评论时还没有头像，之后在个人中心上传头像，播放页评论列表仍显示默认用户图标。
- **影响**：评论区无法反映用户当前头像，容易误判为后端没有返回最新用户资料。
- **根因**：后端 `CommentService.listComments` 会按评论 `userId` 查询当前用户资料，并用当前 `avatarUrl` 覆盖评论创建时的旧快照；但开发配置中的头像公开路径是 `/uploads/avatars/...`，评论区直接把该路径传给 Ant Design `Avatar`。Vite 开发代理只代理 `/api`，因此浏览器请求 `/uploads/avatars/...` 时不会转发到 Spring Boot。个人中心已有 `/api` 前缀补全逻辑，评论区缺少同类处理。
- **解决方案**：在 `videoPlayModel` 中新增 `resolveAvatarUrl`，将后端相对头像路径 `/uploads/avatars/...` 规范化为 `/api/uploads/avatars/...`，保留已带 `/api/` 的路径和外部 HTTP(S) 地址；播放页评论头像统一使用该解析函数。
- **验证方式**：先新增头像路径解析测试并确认失败原因是 `resolveAvatarUrl is not a function`；实现后执行 `npm test -- videoPlayModel.test.ts`，5 个测试通过；执行 `npm run build`，TypeScript 与 Vite 生产构建通过。
- **后续改进**：可抽取全局媒体 URL 解析工具，统一头像、封面、视频等后端相对资源路径，避免不同页面各自拼接 `/api`。

## 37. 评论刷新后数据库已有数据但播放页显示为空

- **日期**：2026-06-18
- **现象**：用户可以在播放页成功发表评论，评论写入数据库后短时间内能通过本地状态显示；刷新页面后，数据库中已有的评论没有回显到前端评论列表。
- **影响**：评论回复、点赞等互动数据看起来像没有持久化，用户会误以为评论发布失败或后端没有返回数据库数据。
- **根因**：后端 `/contents/{contentId}/comments` 按统一格式返回 `ApiResponse<List<CommentInfo>>`，但前端 `contentEngagementApi` 在新增回复/点赞接口时漏掉了 `.then(unwrap)`，实际把 `AxiosResponse<ApiResponse<...>>` 传给播放页。播放页加载时又用 `Array.isArray(loadedComments)` 防御判断，包裹对象不是数组，于是被置为空数组。评分和推荐接口也存在同类解包遗漏风险。
- **解决方案**：恢复评论、创建评论、点赞、删除、评分和推荐接口的统一 `unwrap`；合并前端重复的评论/评分类型声明；补齐新增评论服务签名后的后端测试桩；顺手修复阻断前端构建的登录表单回填字段类型问题。
- **验证方式**：先新增 `frontend/src/services/api.test.ts`，确认修复前 `contentEngagementApi.listComments` 返回包裹对象导致测试失败；修复后 `npm --prefix frontend test -- src/services/api.test.ts` 通过。执行 `npm --prefix frontend run build` 成功。执行 `.\mvnw.cmd test '-Dtest=ContentEngagementServiceTest,ContentEngagementControllerTest,SecurityConfigTest'`，13 个后端相关测试通过。
- **后续改进**：可把 API 层 `unwrap` 约束抽成统一 helper 或生成式客户端，避免新增接口时绕过解包；播放页可以在收到非数组评论数据时记录开发期错误，而不是静默显示空列表。

## 38. 评论列表接口因根评论重复收集返回 500

- **日期**：2026-06-18
- **现象**：播放页请求 `/api/contents/1/comments` 时，浏览器报 `500 Internal Server Error`，前端显示服务器内部错误。
- **影响**：评论数据无法加载，播放页互动区失败；即使数据库中已有评论，用户也看不到评论列表。
- **根因**：`CommentService.listComments` 先查询顶级评论并加入 `allComments`，随后又调用 `findRepliesByRootId(rootId)` 查询该根评论下的记录。旧查询条件只按 `rootId` 过滤，会把根评论本身也查回来，导致同一个 `Comment.id` 在 `Collectors.toMap(Comment::getId, ...)` 中出现两次并抛出 `Duplicate key`，最终被全局异常处理成 500。
- **解决方案**：将 `CommentRepository.findRepliesByRootId` 收紧为只返回 `parentId IS NOT NULL` 的回复记录；同时在服务层构建 `commentMap` 时对重复 id 做兜底，保留第一条记录，避免脏数据或旧查询结果再次把接口打成 500。
- **验证方式**：先新增 `doesNotDuplicateRootCommentWhenLoadingRepliesByRootId` 回归测试，确认修复前复现 `Duplicate key 2`；修复后该测试通过。执行 `.\mvnw.cmd test '-Dtest=ContentEngagementServiceTest,ContentEngagementControllerTest,SecurityConfigTest'`，14 个相关后端测试通过。执行 `npm --prefix frontend run build` 成功。直接请求 `http://localhost:8081/api/contents/1/comments` 返回 HTTP 200 和评论数据。
- **后续改进**：可增加数据库层约束或数据迁移，确保顶级评论 `rootId=id`、回复 `parentId/rootId` 一致；评论树构建可以改为一次查询内容下全部评论后按 parentId 分组，减少 N+1 查询和重复收集风险。

## 39. 注册页前端已改 2-64 但后端仍提示用户名长度 3-64

- **日期**：2026-06-18
- **现象**：注册页 `Login.tsx` 中“名字”和“用户名”的前端表单规则已经改为 2-64 个字符，但提交 2 个字符的注册用户名时仍提示“用户名长度为3-64个字符”。
- **影响**：用户会看到与页面规则不一致的后端错误提示，误以为前端修改没有生效，且 2 字符用户名无法注册。
- **根因**：前端 Ant Design 表单只负责提交前校验；注册接口 `AuthController.register` 仍会对 `AuthDTO.RegisterRequest` 执行 `@Valid`。后端 `username` 字段仍配置为 `@Size(min = 3, max = 64, message = "用户名长度为3-64个字符")`，因此请求到达后端后被 Bean Validation 拦截。
- **解决方案**：将 `AuthDTO.RegisterRequest.username` 的后端校验同步为 `@Size(min = 2, max = 64, message = "用户名长度为2-64个字符")`；新增 `AuthControllerTest.registersWithTwoCharacterUsername`，覆盖 2 字符用户名可以通过控制器入参校验并调用注册服务。
- **验证方式**：先新增控制器测试并确认修复前失败，失败日志显示 `Status expected:<200> but was:<400>`，且默认消息为“用户名长度为3-64个字符”；修改 DTO 后执行 `.\mvnw "-Dtest=AuthControllerTest" test` 通过，1 个测试成功。补充执行 `.\mvnw "-Dtest=AuthServiceTest" test` 时，现有登录相关测试仍失败，失败点为登录身份字段查找与测试桩不一致，和本次注册用户名长度校验不是同一路径，未在本次修复中处理。
- **后续改进**：注册、个人资料、后台创建用户等入口应抽取共享的账号字段约束，避免前端、后端 DTO 和服务层各自维护不同长度规则；同时应清理现有登录测试与 `AuthService.login` 的身份字段变更差异。

## 40. 评论点赞 500 且点赞后部分回复消失

- **日期**：2026-06-18
- **现象**：播放页请求 `POST /api/contents/1/comments/1/like` 时返回 500；同时评论区出现两类不一致表现：有的顶级评论能展示回复但无法点赞，有的顶级评论能点赞但点赞后回复不再展示。
- **影响**：用户无法稳定点赞顶级评论，回复树也会在互动后丢失，评论区状态看起来像数据错乱。
- **根因**：后端旧评论数据存在 `like_count` 为空的记录，`CommentService.toggleLike` 直接执行 `comment.getLikeCount() + 1`，触发 `NullPointerException`，日志显示 `Cannot invoke "java.lang.Integer.intValue()" because ... getLikeCount() is null`。另一个根因是评论列表按 `rootId` 加载回复，旧数据中部分回复 `root_id` 为空时会漏查；前端点赞成功后又用点赞接口返回的单条评论 DTO 直接替换本地评论，而该 DTO 不携带 `replies`，导致已经展示的回复被覆盖为空。
- **解决方案**：后端评论列表改为优先按 `contentId` 一次加载全部评论，再按 `parentId` 组装回复树，并对重复评论 id 去重；`likeCount`、`replyCount` 在 DTO 输出和点赞增减时统一把 null 当作 0。前端新增 `mergeLikedComment`，点赞响应只更新点赞相关字段，同时保留当前评论已有的 `replies`。
- **验证方式**：先新增 `listsRepliesEvenWhenStoredRootIdIsMissing` 和 `likesCommentsWhosePersistedLikeCountIsNull`，确认修复前分别失败为回复列表为空和点赞 NPE；实现后执行 `.\mvnw "-Dtest=ContentEngagementServiceTest,ContentEngagementControllerTest" test`，15 个测试通过。前端先新增 `mergeLikedComment` 测试并确认缺少模块失败；实现后 `npm test -- CommentItem.test.tsx` 通过 2 个测试，`npm run build` 成功。
- **后续改进**：可增加一次性数据迁移，将历史评论的 `like_count/reply_count` 补 0，并按 `parent_id` 回填回复的 `root_id`；评论删除时也应改为按 commentId 删除全部点赞，避免用 `userId = null` 的 JPQL 条件漏删。

## 41. Flyway 自动 baseline 跳过 V1 导致正式化约束和索引未应用

- **日期**：2026-06-20
- **现象**：引入 Flyway 后，配置了 `spring.flyway.baseline-on-migrate: true`，但没有指定 `baseline-version`；本地已有 PostgreSQL schema 在启动测试中被基线到版本 1，而 `V1__initial_schema.sql` 没有实际执行。
- **影响**：如果正式表结构里的索引、唯一约束、check 约束或数据迁移只存在于 `V1`，已有数据库会把版本记录推进到 1 后跳过这些变更，导致 Hibernate `validate` 只能验证列和表的大体存在，不能保证 schema 已真正正式化。
- **根因**：Flyway `baseline-on-migrate` 的默认基线版本为 1，正好等于首个迁移脚本版本；此前把 `V1` 同时当作“空库建表脚本”和“已有库正式化脚本”，但已有库 baseline 后只会执行版本号大于 1 的迁移。后续补偿迁移初版还把 `content_ratings.score` 误写为 1-5 分约束，而业务 DTO 和前端实际是 1-10 分制，真实 PostgreSQL 迁移因此被现有 10 分记录阻断。
- **解决方案**：移除自动 `baseline-on-migrate` 配置，避免新环境继续静默跳过首个迁移；保留 `V1__initial_schema.sql` 作为空库初始化脚本；新增 `V2__formalize_existing_schema` Java migration，在已有 schema 已经 baseline 到 1 的情况下继续补齐索引、唯一索引和 check 约束。评分约束按当前业务规则修正为 `score between 1 and 10`。
- **验证方式**：先新增回归测试复现 baseline 到 1 后不会补约束的问题，确认失败表现为最新版本仍是 1 且 `ck_users_role` 缺失；修复后执行 `.\mvnw.cmd -Dtest=FlywayMigrationTest test`，覆盖空库 `V1+V2` 和已有 schema baseline 后执行 `V2` 两条路径并通过。执行 `.\mvnw.cmd "-Dtest=FlywayMigrationTest,FlywayJpaSchemaValidationTest" test`，3 个测试通过，确认 Flyway schema 可被 Hibernate `validate` 接受。执行 `.\mvnw.cmd -Dtest=VideoAiApplicationTests test`，真实本地 PostgreSQL `video_db` 从版本 1 成功迁移到版本 2，Spring Boot 上下文启动通过。完整 `.\mvnw.cmd test` 此前仍有既有 `AuthServiceTest` 登录断言问题，和本次 Flyway 迁移链路无关。
- **后续改进**：后续把历史 Hibernate schema 纳入 Flyway 时，应明确区分“空库初始化迁移”和“已有库补偿迁移”；如必须 baseline，应把后续正式化变更放在更高版本号中，并在迁移测试里模拟已基线数据库，防止同类跳过再次发生。

## 42. AuthService 登录测试仍按 username mock 导致安全核心回归误报

- **日期**：2026-06-20
- **现象**：`AuthServiceTest.rejectsDisabledAccount` 期望禁用账号返回“账号已被禁用，请联系管理员”，实际返回“用户名或密码错误”；`logsInWithNormalizedEmail` 报 Mockito `UnnecessaryStubbingException`，指出 `findByUsername("VIEWER@EXAMPLE.COM")` stub 未被使用。
- **影响**：认证服务是登录安全核心路径，测试如果仍按旧身份字段 mock，会掩盖当前登录合同，导致禁用账号、邮箱登录、密码校验和单设备会话创建等关键流程的回归测试失真。
- **根因**：`AuthService.login` 当前使用 trim 后的 `displayName` 作为第一查找条件，再 fallback 到 `findByEmailIgnoreCase(identity)`；测试仍停留在旧的 username 登录语义，给 `findByUsername` 配置 mock。禁用账号测试因此没有查到用户，直接进入“用户名或密码错误”；邮箱登录测试则留下了实现不会调用的冗余 stub。
- **解决方案**：将 `logsInWithNormalizedEmail` 的首个查找 mock 改为 `findByDisplayName("VIEWER@EXAMPLE.COM")` 返回空，再验证 fallback 到 `findByEmailIgnoreCase`；将 `rejectsDisabledAccount` 改为通过 `findByDisplayName("viewer")` 命中禁用用户，保留“密码正确但账号禁用时返回禁用消息”的业务断言。
- **验证方式**：先执行 `.\mvnw.cmd -Dtest=AuthServiceTest test` 复现 1 个失败和 1 个 Mockito 错误；修正后再次执行同一命令，5 个认证服务测试全部通过。随后执行 `.\mvnw.cmd test`，后端完整 73 个测试全部通过。
- **后续改进**：登录身份字段如果继续演进，应同步更新 `AuthServiceTest` 中对仓储查找顺序的 verify 断言，并优先覆盖禁用账号、密码错误、邮箱大小写、前后空格和会话创建这些安全关键分支。

## 43. Spring Boot 测试依赖本地 video_db 掩盖 CI 空库迁移风险

- **日期**：2026-06-20
- **现象**：`VideoAiApplicationTests` 只是普通 `@SpringBootTest`，没有测试 profile 或测试 datasource；执行 `.\mvnw.cmd test` 时会继承主配置中的 `jdbc:postgresql://localhost:5432/video_db`。本地库已经被手动 baseline 并迁移到版本 2，因此本地通过不能证明 CI 空库也能通过。
- **影响**：CI 或生产验证环境如果是空库、不同 schema 状态，`mvnw test` 会执行完整 Flyway 迁移链路；一旦迁移脚本与本地基线后的 schema 假设不一致，PR 会在 CI 阶段失败，或者更糟糕地让本地测试遗漏空库初始化问题。
- **根因**：测试上下文没有隔离数据源，`spring.profiles.active: dev` 和主 datasource 位于 `application.yml`，导致缺少测试覆盖时默认连接开发 PostgreSQL。此前的全量测试验证的是当前开发机的 `video_db`，不是可重复的空库迁移流程。
- **解决方案**：新增 `src/test/resources/application-test.yml`，为测试 profile 显式配置 H2 PostgreSQL mode、Hibernate `validate` 和 Flyway migration location；为 `VideoAiApplicationTests` 添加 `@ActiveProfiles("test")`，并断言实际 JDBC URL 包含 `jdbc:h2:mem:video_ai_test` 且 `flyway_schema_history` 最新成功版本为 `2`。同时让 `FlywayJpaSchemaValidationTest` 使用 `test` profile，避免激活 dev 初始化逻辑。
- **验证方式**：先修改 `VideoAiApplicationTests` 加入 H2 URL 断言，在未提供 `application-test.yml` 时执行 `.\mvnw.cmd -Dtest=VideoAiApplicationTests test`，失败信息显示实际连接仍是 `jdbc:postgresql://localhost:5432/video_db`。补充测试 profile 后再次执行同一命令通过，日志显示空 H2 schema 执行 `V1`、`V2` 到版本 2。随后执行 `.\mvnw.cmd "-Dtest=VideoAiApplicationTests,FlywayJpaSchemaValidationTest,FlywayMigrationTest" test`，4 个迁移/上下文测试通过；最终执行 `.\mvnw.cmd test`，73 个后端测试全部通过。
- **后续改进**：可以继续把所有需要 Spring 上下文但不需要真实外部依赖的测试统一标注 `test` profile，减少 dev 配置泄漏；CI 也应显式执行后端完整测试，确保每个 PR 都从空库验证 Flyway 脚本。

## 44. 旧 Video 模块未完全下线导致后台统计仍依赖 videos

- **日期**：2026-06-20
- **现象**：作品主流程已经改为 `contents`、`seasons`、`episodes`，但管理后台统计仍调用前端 `videoApi.listPublished()`，后端也保留 `/videos` 控制器、`VideoService`、`VideoRepository`、`VideoDTO`、`Video` 实体和 `videos` 表。
- **影响**：旧 `videos` 数据即使已经不参与新播放、审核、评论评分链路，仍可能影响后台统计；同时项目存在两套作品入口，后续维护容易误用旧接口。
- **根因**：内容模型迁移到 `Content/Episode` 后，只完成了主业务页面切换，没有同步下线旧 `Video` 应用层 API 和数据库表。
- **解决方案**：管理后台统计改为 `contentApi.listPublished()`；删除前端 `videoApi` 和旧视频类型；删除后端 `VideoController`、`VideoService`、`VideoRepository`、`VideoDTO`、`Video` 实体；移除 `/videos/public/**` 放行规则；新增 `V3__drop_legacy_videos_table.sql` 删除旧 `videos` 表。
- **验证方式**：先修改 `FlywayMigrationTest` 期望迁移后 `videos` 表不存在并确认失败；新增 V3 后 `FlywayMigrationTest` 通过。随后执行 `./mvnw.cmd test`，73 个后端测试全部通过；执行 `npm --prefix frontend run build`，前端 TypeScript 与 Vite 生产构建通过。残留搜索未再命中 `VideoController`、`VideoService`、`VideoRepository`、`VideoDTO`、`videoApi`、`/videos/public`。
- **后续改进**：如果需要保留历史旧视频，应先迁移到 `contents/episodes` 再执行删表；后续模型替换时应把入口、后台统计、权限放行、DTO、实体和 Flyway 清理列入同一张检查清单。
## 45. MinIO/HLS 链路引入后 Spring 构造器与 Flyway 版本断言导致全量测试失败

- **日期**：2026-06-20
- **现象**：实现 MinIO + FFmpeg HLS 异步转码链路后，单项服务测试可以通过，但执行 `mvnw.cmd test` 时出现 2 类失败：Spring Boot 上下文启动时报 `MediaStorageService: No default constructor found`；迁移测试仍期望最新 Flyway 版本为 `3`，实际已迁移到 `4`。
- **影响**：后端应用上下文无法在完整测试中启动，导致 `/media` 控制器和 Flyway/JPA schema 校验无法证明新链路可集成运行；Flyway 测试的旧版本断言也会误判新增迁移为失败。
- **根因**：`MediaStorageService` 为了兼容单元测试保留了 1 参数构造器，同时新增了生产用的 2 参数构造器，但没有用 `@Autowired` 标注 Spring 应选择的构造器，Spring 无法明确进行构造器注入。迁移层面新增 `V4__add_media_transcode_jobs.sql` 后，`FlywayMigrationTest` 和 `VideoAiApplicationTests` 仍硬编码最新版本为 `3`，且没有检查新表。
- **解决方案**：给 `MediaStorageService(MediaStorageProperties, TranscodeService)` 标注 `@Autowired`，保留 1 参数构造器仅供窄单元测试使用；将 Flyway 相关测试的最新版本断言更新为 `4`，并显式检查 `media_transcode_jobs` 表与 `hls_url` 字段存在。
- **验证方式**：先执行 `mvnw.cmd test` 复现 Spring 构造器错误和 Flyway 版本断言失败；修复后再次执行 `mvnw.cmd test`，82 个后端测试全部通过。随后执行 `mvnw.cmd -DskipTests package`，后端 JAR 打包成功。前端补齐转码作业轮询后，执行 `npm --prefix frontend test`，10 个测试文件 41 个测试通过；执行 `npm --prefix frontend run build`，TypeScript 与 Vite 生产构建通过。`docker compose -f docker-compose.dev.yml config` 和 `docker compose -f docker-compose.yml config` 均可解析，仅提示 Compose `version` 字段已弃用。
- **后续改进**：当新增 Flyway 迁移时，测试不要只断言版本号，还应断言新 schema 对象；服务类如果为了单元测试保留辅助构造器，生产构造器必须显式标注注入语义，或改用测试专用工厂方法避免与 Spring 构造器选择混淆。
